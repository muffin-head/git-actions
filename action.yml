name: 'Hello world'
description: 'greet someone'
inputs:
	who-to-greet:
		description: 'who to greet'
		required: true
		default: 'world'
outputs:
	time:
		description: 'The time we greeted you'
	runs:
		using: 'node12'
		main: 'index.js'
